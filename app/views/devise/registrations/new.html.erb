<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <% if params[:plan].to_i == 2 %>
        <h1>PRO PLAN!</h1>
      <% else %>
        <h1>basic junk.</h1>
      <% end -%>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      
      <h2>Sign up</h2>

      <%if params[:plan].to_i == 2 %>
        <%= render "pro_form"%>
      <%else%>
        <%= render "basic_form"%>
      <% end -%>
        
      <%= render "devise/shared/links" %>
    </div>   
  </div>
</div>